// import {  SuspendedDeliveryList } from '@cookers/models';
// import { getAxiosInstance } from '@cookers/services';

// export const fetchSuspendedDeliveryViewData = async (customerCode: number): Promise<SuspendedDeliveryList> => {
//     // const storeData = store.getState().globalMaster.globalMasterData;
//     const URL = `suspended-delivery/quick-view`;
//     const response = await getAxiosInstance().get<SuspendedDeliveryList>(URL + '/' + customerCode);
//     return response.data;
// };