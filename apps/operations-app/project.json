{
  "name": "operations-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/operations-app/src",
  "projectType": "application",
  "tags": ["operations-app"],
  "// targets": "to see all targets run: nx show project operations-app --web",
  "targets": {
    "serve": {
      "options": {
        "port": 4500
      }
    }
  }
}
